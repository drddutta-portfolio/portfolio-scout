name: Deploy backfill-market-eod

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "edge-functions/backfill-market-eod/**"
      - ".github/workflows/deploy-backfill-market-eod.yml"

permissions:
  contents: read

concurrency:
  group: deploy-backfill-market-eod-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to Supabase
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Stage function in Supabase CLI layout
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .supabase-deploy
          mkdir -p .supabase-deploy/supabase/functions
          cp -R edge-functions/backfill-market-eod .supabase-deploy/supabase/functions/backfill-market-eod
          cat > .supabase-deploy/supabase/config.toml <<'EOF'
          [functions.backfill-market-eod]
          verify_jwt = true
          EOF

      - name: Deploy backfill-market-eod
        working-directory: .supabase-deploy
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$SUPABASE_ACCESS_TOKEN"
          test -n "$SUPABASE_PROJECT_ID"
          supabase functions deploy backfill-market-eod \
            --project-ref "$SUPABASE_PROJECT_ID"
